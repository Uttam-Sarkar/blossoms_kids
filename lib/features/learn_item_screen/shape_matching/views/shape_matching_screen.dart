import 'package:flutter/material.dart';

class ShapeMatchingScreen extends StatelessWidget {
  const ShapeMatchingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("Shape Matching Screen")
      ),
    );
  }
}
