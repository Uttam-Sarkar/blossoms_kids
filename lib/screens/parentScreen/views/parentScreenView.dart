import 'package:flutter/material.dart';

class ParentScreenView extends StatelessWidget {
  const ParentScreenView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Parent Screen")),
    );
  }
}
