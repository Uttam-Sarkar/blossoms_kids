import 'package:flutter/material.dart';

class SocialSkillScreen extends StatelessWidget {
  const SocialSkillScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("Social Skill Screen")
      ),
    );
  }
}
