import 'package:flutter/material.dart';

class PsychologicalScreen extends StatelessWidget {
  const PsychologicalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Text("Psychological Screen")
      ),
    );
  }
}

